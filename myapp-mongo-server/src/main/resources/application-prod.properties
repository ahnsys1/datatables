# Use MM/dd/yyyy for LocalDate serialization/deserialization
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.serialization.write-dates-as-timestamps=false
# ===================================================================
# APPLICATION CONFIGURATION
# ===================================================================
spring.application.name=myapp-mongo-server
server.port=9090

# ===================================================================
# DEVELOPMENT DATABASE (MongoDB)
# ===================================================================
# These properties are used for local development.
# For tests, the connection is managed by Testcontainers.
#codebase make useage of Date/LocalDate claases to use a format yyyy/MM/dd  This will nbe consistent consistent accross whole codebase. #codebase make useage of Date/LocalDate claases to use a format yyyy/MM/dd  This will nbe consistent consistent accross whole codebase. spring.data.mongodb.uri=mongodb://localhost:27017/postgres
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=datatablesclear
spring.data.mongodb.username=postgres
spring.data.mongodb.password=test
