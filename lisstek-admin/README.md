# lisstek-admin

Samostatny administracni backend pro Listek na Spring Boot 4.1.0.

## Spusteni

```bash
mvn spring-boot:run
```

Aplikace bezi na `http://localhost:8090`. Databazove prihlaseni lze nastavit promenymi `DB_URL`, `DB_USERNAME` a `DB_PASSWORD`.
