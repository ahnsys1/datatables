spring:
  application:
    name: lisstek-admin
  jpa:
    open-in-view: false
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/listek}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    enabled: false

server:
  port: ${PORT:8090}
